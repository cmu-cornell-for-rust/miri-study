{
	"name": "Miri-Study",
	"build": {
		"dockerfile": "Dockerfile",
		"target": "base",
		"context": ".."
	},
      "runArgs": [
	"--cap-add=SYS_ADMIN", "--cap-add=PERFMON", "--privileged"
  ],
	// Mount SSH folder
	"mounts": [
		"source=${localEnv:HOME}/.ssh,target=/root/.ssh,readonly,type=bind"
	],
	"remoteEnv": {
		"PATH": "${containerEnv:PATH}:${containerWorkspaceFolder}"
	},
	"features": {
		"ghcr.io/devcontainers/features/github-cli:1": {
			"version": "latest"
		}
	},
	"customizations": {
		"vscode": {
			"settings": {
				"python.defaultInterpreterPath": "${containerWorkspaceFolder}/.venv/bin/python",
				"python.terminal.activateEnvironment": true
			},
			"extensions": [
				"rust-lang.rust-analyzer",
				"ms-python.python",
				"ReprEng.csv"
				// ,"ms-vscode.cpptools"
			]
		}
	},
	"postCreateCommand": "if [ -x .venv/bin/python ]; then .venv/bin/python -m pip install -U pip ipykernel matplotlib; fi"
}